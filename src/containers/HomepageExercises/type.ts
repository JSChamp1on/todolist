export interface IReadonlyProps {
    readonly methodMgTest: null | {
        data: [];
    }
};

export interface IExercisesItem {
    title: string;
    small_image: string;
    large_image: string;
    steps: [];
};